library("tidyverse")
library("tidyverse")
data("mtcars")
mtcars
class(mtcars)
w = c(10, 20 , 50, 5.32)
class(w)
class(mtcars)
head(mtcars) # head(...) to see only first 6 rows
#Q1: How many observations or data points?
nrow(mtcars)
#Q2: How many variables?
ncol(mtcars)
head(mtcars) # head(...) to see only first 6 rows
#Q3: What are the column names?
colnames(mtcars)
#Q4: How can I see only the "mpg" colums?
## <<dataframe_name>>$<<column_name>>
mtcars$mpg
mtcars$cyl
#Q5: How can I see the max and min of "mpg" column?
max(mtcars$mpg)
min(mtcars$mpg)
#Q5: How can I plot the "mpg" column against "disp" column?
plot(mtcars$disp, mtcars$mpg)
butterfly = c(4.3, 4.4,4.0,4.5,3.8,3.9,4.1,3.6,3.7,3.3,3.5,4.2   )
mean(butterfly)
butterfly = c(4.3, 4.4,4.0,4.5,3.8,3.9,4.1,3.6,3.7,3.3,3.5,4.2   )
mean(butterfly)
median(butterfly)
order(butterfly)
butterfly[order(butterfly)]
order(butterfly)
butterfly[order(butterfly)]
var(butterfly)
sd(butterfly)
sqrt(  var(butterfly) )
